import 'package:flutter/material.dart';

class HomeSpeedDialButton extends StatelessWidget {
  const HomeSpeedDialButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
