import 'package:flutter/material.dart';

class TabBarDemosPage extends StatelessWidget {
  const TabBarDemosPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
